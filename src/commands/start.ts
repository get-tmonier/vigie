export function start() {
  console.log('tmonier daemon starting...');
  console.log('Listening for connections (WebSocket placeholder)');

  // Keep the process alive
  setInterval(() => {}, 1 << 30);
}
