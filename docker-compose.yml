services:
  postgres:
    image: postgres:17
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: tmonier
      POSTGRES_USER: tmonier
      POSTGRES_PASSWORD: tmonier
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
