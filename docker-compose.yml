services:
  postgres:
    image: postgres:17
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: vigie
      POSTGRES_USER: vigie
      POSTGRES_PASSWORD: vigie
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
